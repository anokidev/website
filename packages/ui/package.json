{
  "name":        "@website/ui",
  "version":     "1.0.0",
  "license":     "MIT",
  "private":     true,

  "description": "Styles and components for use in svelte websites",

  "module":      "index.ts",
  "svelte":      "index.ts",

  "scripts": {
    "repair:format": "prettier --write --ignore-path=../../.prettierignore .",
    "repair:lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \".\""
  },
  
  "devDependencies": {
    "eslint-config-custom": "*",
    "svelte": "^3.9.2"
  },

  "type": "module"
}